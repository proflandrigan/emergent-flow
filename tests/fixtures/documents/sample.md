# Sample Document

This is a tiny checked-in markdown fixture for testing `ef.data.load_documents` (Epic 16,
Story 20).

It has a couple of paragraphs so a small `chunk_size` in tests produces more than one chunk,
exercising the chunk-boundary logic deterministically.

## Second Section

Second section body text, still part of the same fixture file.
